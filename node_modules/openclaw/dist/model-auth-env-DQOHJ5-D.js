import { r as resolvePluginSetupProvider } from "./setup-registry-dR9mJPAQ.js";
import { t as getShellEnvAppliedKeys } from "./shell-env-BNKFe8H4.js";
import { o as resolveProviderIdForAuth } from "./provider-env-vars-CB35Yqpw.js";
import { t as normalizeOptionalSecretInput } from "./normalize-secret-input-C0Z6NpKb.js";
import { _ as resolveProviderEnvApiKeyCandidates } from "./model-auth-markers-BrDFmU-z.js";
import { getEnvApiKey } from "@mariozechner/pi-ai";
//#region src/agents/model-auth-env.ts
function resolveEnvApiKey(provider, env = process.env) {
	const normalized = resolveProviderIdForAuth(provider, { env });
	const candidateMap = resolveProviderEnvApiKeyCandidates({ env });
	const applied = new Set(getShellEnvAppliedKeys());
	const pick = (envVar) => {
		const value = normalizeOptionalSecretInput(env[envVar]);
		if (!value) return null;
		return {
			apiKey: value,
			source: applied.has(envVar) ? `shell env: ${envVar}` : `env: ${envVar}`
		};
	};
	const candidates = Object.hasOwn(candidateMap, normalized) ? candidateMap[normalized] : void 0;
	if (Array.isArray(candidates)) {
		for (const envVar of candidates) {
			const resolved = pick(envVar);
			if (resolved) return resolved;
		}
		return null;
	}
	if (normalized === "google-vertex") {
		const envKey = getEnvApiKey(normalized);
		if (!envKey) return null;
		return {
			apiKey: envKey,
			source: "gcloud adc"
		};
	}
	const setupProvider = resolvePluginSetupProvider({
		provider: normalized,
		env
	});
	if (setupProvider?.resolveConfigApiKey) {
		const resolved = setupProvider.resolveConfigApiKey({
			provider: normalized,
			env
		});
		if (resolved?.trim()) return {
			apiKey: resolved,
			source: resolved === "gcp-vertex-credentials" ? "gcloud adc" : "env"
		};
	}
	return null;
}
//#endregion
export { resolveEnvApiKey as t };
