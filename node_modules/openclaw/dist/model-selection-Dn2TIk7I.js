//#region extensions/qa-lab/src/model-selection.ts
function normalizeQaProviderMode(mode) {
	return mode === "live-openai" ? "live-frontier" : mode;
}
function defaultQaModelForMode(mode, options) {
	if (normalizeQaProviderMode(mode) === "live-frontier") return options?.preferredLiveModel ?? "openai/gpt-5.4";
	return options?.alternate ? "mock-openai/gpt-5.4-alt" : "mock-openai/gpt-5.4";
}
function splitQaModelRef(ref) {
	const slash = ref.indexOf("/");
	if (slash <= 0 || slash === ref.length - 1) return null;
	return {
		provider: ref.slice(0, slash),
		model: ref.slice(slash + 1)
	};
}
function isQaFastModeModelRef(ref) {
	return splitQaModelRef(ref)?.provider === "openai";
}
function isQaFastModeEnabled(selection) {
	return isQaFastModeModelRef(selection.primaryModel) || isQaFastModeModelRef(selection.alternateModel);
}
//#endregion
export { splitQaModelRef as a, normalizeQaProviderMode as i, isQaFastModeEnabled as n, isQaFastModeModelRef as r, defaultQaModelForMode as t };
